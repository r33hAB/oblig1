package com.example.quizapp

import androidx.test.ext.junit.runners.AndroidJUnit4
import org.junit.Test
import org.junit.runner.RunWith

@RunWith(AndroidJUnit4::class)
class QuizTest {

    @Test
    fun quiz_score_logic_is_todo() {
        // TODO (required): Set up predictable quiz data before each test.
        // TODO (required): Verify that the score is updated correctly after answering.
        // TODO (required): Verify quiz state after moving to the next question.
        // TODO (required): Clean up test data after the test if needed.
        // TODO (optional): Use fake or test-only gallery data to make quiz questions deterministic.
    }
}
